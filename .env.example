# =============================================================================
# Docker Compose Configuration
# =============================================================================

# Image tag for Docker builds (default: latest)
TAG=latest

# Application Ports

APP_PORT=3000          # Frontend web server port
API_PORT=8000          # Backend API server port

# =============================================================================
# Frontend Environment Variables (build-time)
# =============================================================================

# TMDB API Key (required)
# Get your API key from: https://www.themoviedb.org/settings/api
VITE_TMDB_KEY=your_tmdb_api_key_here

# OMDb API Key (optional - used as fallback for movie posters)
# Get your API key from: https://www.omdbapi.com/apikey.aspx
VITE_OMDB_KEY=your_omdb_api_key_here

# Backend API URL
# For Docker: http://backend:8000 (set automatically in docker-compose)
# For local dev: http://localhost:8000
VITE_API_URL=http://localhost:8000

# Environment identifier (optional)
VITE_ENV=development

# =============================================================================
# Legacy / Deprecated
# =============================================================================

# Google Sheets API URL (deprecated - replaced by VITE_API_URL)
# Keep for backwards compatibility if you still have the old sheets.js file
# VITE_SHEETS_API=your_google_sheets_api_url_here

# TMDB Image URLs are hardcoded in the code (not configurable via env vars)
# Defaults: https://image.tmdb.org/t/p/w154 (small) and w300 (large)

# =============================================================================
# Backend Environment Variables (runtime)
# =============================================================================

# Backend server port (default: 8000)
PORT=8000

# CORS origin (default: http://localhost:3000)
CORS_ORIGIN=http://localhost:3000

# Database path (optional, default: ./data/cinematheque.db)
# DB_PATH=./data/cinematheque.db
